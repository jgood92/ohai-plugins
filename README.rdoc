= DESCRIPTION:

Ohai plugins to recognise windows programs and services.

= ATTRIBUTES:
ohai.plugins  =  array of files to push 
ohai.pligin_path = location to install them (this needs to be setup in your chef client.rb)


= REQUIREMENTS:
Need to be placed in the ohai cookbook under files/default/plugins
